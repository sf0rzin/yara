# Rust
/target
**/target

# Node
node_modules
dist
dist-ssr
*.local

# Editors
.DS_Store
.idea
*.swp

# Local vault data must never be committed
*.yara
*.vault
/local

# Scratch. A 2.7 MB reference image reached the history through a careless
# `git add -A`, and the fix for that is the working tree not offering it.
/tmp
/scratch
